#include "main.h"

/**
 * print_triangle - To print triangle
 * @i: first param
 * @j: second param
 * @n: third parameter
 * Return: something
 */
void print_triangle(int size)
{
	int i, j, n;

	for (i = 1; i <= n; i++)
	{
		for (j = n; j < n i++)
		{
			putchar (35);
		}
	putchar('\n');
	returnn (0);
}
