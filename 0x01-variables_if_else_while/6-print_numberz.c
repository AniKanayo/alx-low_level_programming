#include <stdio.h>

/**
 * main - Entry point
 * author: micky
 * Return: Always 0 (Success)
 */
int main(void)
{
	int micky;

	for (micky = 48; micky < 58; micky++)
		putchar(micky);
	putchar('\n');
	return (0);
}
