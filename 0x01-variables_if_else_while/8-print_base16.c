#include <stdio.h>

/**
 * main - Entry point
 * autor: kanayo
 * Return: Always 0 (Success)
 */
int main(void)
{
	int micky;
	char knayo;

	for (micky = 48; micky < 58; micky++)
		putchar(micky);
	for (kanayo = 'a'; kanayo <= 'f'; kanayo++)
		putchar(kanayo);
	putchar('\n');
	return (0);
}