#include <stdio.h>

/**
 * main - Entry point
 * author: micky
 * Return: Always 0 (Success)
 */
int main(void)
{
	int micky;

	for (micky = 0; micky < 10; micky++)
	{
		printf("%d", micky);
	}
	printf("\n");
	return (0);
}